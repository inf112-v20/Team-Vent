package inf112.skeleton.app.model;

public class GameState {
    StateInfo[] stateInfos;
    int added;
    public GameState (int size) {
        stateInfos = new StateInfo[size];
        int added = 0;
    }

    public void add (StateInfo stateInfo) {
        StateInfo[added] = stateInfo.
    }

}
